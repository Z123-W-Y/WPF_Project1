<UserControl x:Class="Project.Views.Pages.StartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Project.Views.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!--上区域标签选项-->
        <Grid Grid.Row="0" Margin="0,27,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            
            <!--右侧路径区域-->
            <TextBox  Grid.Column="2" Style="{StaticResource TextBoxStyle}" 
                      IsReadOnly="True" 
                      Text="D: \ TPL_File \ 2308161616CS.TPL"
                      BorderBrush="#7c909d"
                      Width="298"
                      Height="32"
                      Padding="16,4,0,0"
                      Margin="0,0,16,4"/>
    
            <!--左侧Tab标签导航-->
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <RadioButton x:Name="btnMaterial"  Content="料号管理" GroupName="StartPage" IsChecked="True" Style="{StaticResource TabButtonStyle}" Margin="0,0,4,0" Click="btnMaterial_Click"/>
                <RadioButton x:Name="btnThickness" Content="测厚管理" GroupName="StartPage" Style="{StaticResource TabButtonStyle}" Margin="0,0,4,0" Click="btnThickness_Click"/>
                <RadioButton x:Name="btnHistory" Content="历史查询" GroupName="StartPage" Style="{StaticResource TabButtonStyle}" Margin="0,0,4,0" Click="btnHistory_Click"/>
                <RadioButton x:Name="btnParameter" Content="参数设置" GroupName="StartPage" Style="{StaticResource TabButtonStyle}" Margin="0,0,4,0" Click="btnParameter_Click"/>
                <RadioButton x:Name="btnCamera" Content="相机管理" GroupName="StartPage" Style="{StaticResource TabButtonStyle}" Margin="0,0,4,0" Click="btnCamera_Click"/>
                <RadioButton x:Name="btnOnline" Content="在线设置" GroupName="StartPage" Style="{StaticResource TabButtonStyle}" Margin="0,0,0,0" Click="btnOnline_Click"/>
            </StackPanel>
        </Grid>
        
        <!--下内容区-->
        <Grid Grid.Row="1">
            <Border Background="#364D5B"
                    BorderBrush="#8c9aa3"
                    BorderThickness="1" >
                <ContentControl x:Name="SystemContent"/>
            </Border>
        </Grid>
    </Grid>
</UserControl>
